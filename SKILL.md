---
name: leadingspace-talking-head-editor
description: 适用于 9:16 中文口播视频的语义剪辑与包装：整理口播、安排 B-roll 和 MG/PPT 信息页、同步动画与音效、制作字幕和同源圆形人物窗，并按样稿锁定视觉风格。
---

# 口播剪辑skill

## 什么时候使用

用户要把中文口播视频剪成适合视频号、抖音、小红书的 9:16 竖屏成片时使用。适用于知识分享、品牌观点、办公空间、产品介绍和人物出镜口播。若用户提供了 MG 卡片、PPT 信息页或字幕样稿，先读取样稿的视觉规则，再开始包装。

## 输入检查

- 主口播视频或独立的人物画面，优先保留原声作为叙事主线。
- 可用 B-roll、图片、屏幕录制或允许生成的补充素材。
- 目标平台、时长限制、品牌名称和必须保留的信息。
- 可选的 MG/PPT/字幕样稿：优先接受 1–3 张截图或 5–15 秒参考片段。样稿没有提供时，使用本文的默认风格，并在交付前说明默认风格。

## 核心工作流

### 1. 先整理内容，再做包装

1. 转写并校正口播，去掉明显口误、重复句、空白和无信息停顿。
2. 保留原意，按“钩子 → 观点/问题 → 证据或例子 → 结论/行动”重排；删除后必须重新通读，保证上下句衔接自然。
3. 按语义段切分，而不是按固定秒数切片。相近语义继续使用同一画面，避免为了节奏硬切。
4. 先标出每段的关键句、关键词、信息点数量和需要强调的数字或结论。

### 2. B-roll 决策模块

对每个语义段依次判断：

1. 这句话是在讲具体场景、人物行为、产品/物体、抽象概念，还是数据/观点？
2. B-roll 是否会让观众更容易“看到他说的内容”？只有能增强理解时才插入。
3. 选材优先级固定为：语义高度匹配 → 场景匹配 → 情绪/氛围匹配。素材漂亮但不相关时放弃。
4. 找不到合适 B-roll 时按顺序退回：人物口播 → MG 信息图 → 大字关键词/标题 → 简单图形动画。
5. B-roll 出现时默认承担完整画面，按 1080×1920 竖屏全画幅铺开；时长覆盖这段语义，下一段语义明显变化时再换。
6. B-roll 期间如需保留说话人，把同一时间线的人物源复制到上层做圆形人物窗。人物必须与当前口播时间一致，圆窗静音，不能使用另一段错位的人物素材。
7. 同类相似片段之间不加转场；使用干净切点，靠语义、节奏和版式变化完成段落切换。

### 3. 信息点和画面形态

- 一段信息点超过 2 个：制作 9:16 满屏 PPT 式信息页。文字放大并占据视觉中心区域，使用清晰的层级、留白和分组，不要缩成左上角的小块。
- 信息点不超过 2 个：根据内容选择卡片、单句大字、数字强调、图形关系或简短 MG，不要把所有内容都套成同一种卡片。
- MG 的形式服从信息：卡片用于分组和对比，纯文字用于观点和结论，图形/图标用于关系或步骤，简单动效用于节奏强调。
- 所有信息页和 MG 都必须避开人物头部、脸部和字幕安全区；先确定人物和字幕占用区域，再放置元素。
- 满屏 PPT 或满屏 MG 时，人物使用 300×300 的正圆窗口，放在文字排版上方或不影响阅读的安全位置。圆形必须用真实圆形 Mask 验证，不能只依赖圆角数值。

### 4. 样稿风格选择与锁定

如果用户提供样稿，先提取以下设计令牌，再制作一个代表性 MG 作为样稿校验：

- 背景、主色、强调色、文字色和明度关系。
- 字体气质、字号层级、字重、行距、对齐和最大行宽。
- 卡片形状、圆角、边框、阴影、网格、留白和安全边距。
- 元素进出方式、动画速度、节拍点、镜头层级和音效类型。

用户确认或样稿已足够明确后，锁定这些令牌，整条视频保持一致；需要变化时只改变信息形态，不随意改变品牌规则。可用 design-md 选择接近的设计系统作为起点，再用 HyperFrames/Remotion 实现可控的布局和动画。

没有样稿时，默认使用轻暖纸张风格：米白背景、白色内容面板、橙色强调、深灰正文、编辑感留白和清晰网格；不要使用纯黑满屏背景。左上角可放低存在感的 `Leadingspace` 标识，但不能抢正文层级。

### 5. 动画、音效与口播同步

- 动画节奏由口播决定：在关键词、数字、结论或信息段落开始的时间点进场；口播转折或信息结束时退场。
- 把每个信息点视为一个可验证的时间事件，使用淡入、上移、缩放、擦入、数字递进或轻微弹性，不为了炫技堆叠动效。
- MG 动画和字幕不能提前泄露下一句信息，也不能晚于口播重点太久。
- 每一个有明显运动的元素配一个匹配的轻量音效，例如 whoosh、soft hit、pop 或 UI click；静态元素不额外加音效。
- 音效低于人声，不能遮挡口播；相邻相似动画不重复堆叠过密音效。

### 6. 人物圆窗标准

- 圆窗使用当前时间线的同源口播素材和正确的源时间偏移。
- 默认尺寸 300×300，正圆 Mask，留出安全边距；优先放在信息页文字上方或空白角落，避开脸、正文和字幕。
- 从人物全屏画面缩到圆窗时做一次短暂、平滑的缩小动画；进入圆窗后保持稳定，不反复跳动。
- 圆窗视频必须静音，口播声音只保留主音轨。

### 7. 字幕标准

- 只显示一行，按自然短语断行；不能把一个连续词语或固定搭配拆开。
- 以语义和呼吸点切分，单行过长时重新组织短语，而不是硬挤小字号。
- 默认白字、黑色阴影、不描边；使用清晰的无衬线字体和稳定的底部安全区。
- 重点词、数字和结论可使用橙色并适度放大，保持整行阅读顺畅，不让高亮覆盖人物脸部。
- 检查每个字幕卡的实际渲染行数、溢出、时间边界和与口播的同步。

## 质量检查

交付前逐项检查：

- 画面为 9:16，主体、字幕和关键信息没有被裁切。
- 口播逻辑连贯，删减处听感自然，语音没有明显跳切或重复。
- 每个 B-roll 都能解释当前语义；不相关素材已退回人物、MG 或文字方案。
- 信息点超过 2 个的段落确实使用满屏 PPT 式信息页，文字足够大且视觉平衡。
- MG、PPT、B-roll 与口播时间点一致；相似片段没有多余转场。
- 圆窗为真实 300×300 正圆、同源对齐、静音，且没有压住人物头部、字幕或正文。
- 字幕始终单行、自然断句、白字黑影、无描边，重点橙色放大但不喧宾夺主。
- 每个明显动画都有合适音效，整体音效低于人声。
- 从头、每个主要信息页中段、人物圆窗切换处和结尾各抽帧复核，再看一遍完整成片。

## 输出

交付可发布的 9:16 成片，并保留可继续修改的时间线。说明使用的样稿风格或默认风格、B-roll/MG 的主要决策、字幕规则和已完成的质量检查。后续项目只需提供口播素材；如果想换视觉风格，先提供样稿，按“提取令牌 → 做代表性 MG → 锁定全片”的步骤更新。
